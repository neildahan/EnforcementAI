// Client branding shown in the page headers (co-branded with the "סוכן אכיפה"
// product logo in the sidebar). This is the single source of truth: when the
// in-app Settings screen lands, it writes here (logo upload + display name),
// so swapping a client's logo is a one-field change — nothing else moves.
export const BRAND = {
  clientLogo: "/ec-logo.svg",
  clientName: "", // optional display name; used as the logo's alt text
};
