# Used by `vite build` (production) → the build that gets pushed to Power Platform.
# Live Dataverse data via the generated connector service. (Local `npm run dev`
# stays on the mock, since it has no value here.) No secrets in this file.
VITE_USE_MOCK=false
